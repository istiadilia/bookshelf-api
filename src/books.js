const books = [];

// books data in array object
{
    name: String;
    year: number;
    author: String;
    summary: String;
    publisher: String;
    pageCount: number;
    readPage: number;
    reading: Boolean;
}

module.exports = books;